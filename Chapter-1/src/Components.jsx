export default function Components(x){
    return (
        <>
        <h5>Passing the Prop in to the component the id is {x.id} and the title is {x.title}</h5>
        <p> Here we have learned how to send props to components id and the title is the Props</p>
        </>
    )
}

